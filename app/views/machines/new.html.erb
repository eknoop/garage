<%- model_class = Machine -%>
<div class="page-header">
  <h1><%=t '.title', :default => [:'helpers.titles.new', 'New %{model}'], :model => model_class.model_name.human.titleize %></h1>
</div>
<%= form_for [current_user, @machine], :html => { :class => "form-horizontal machine" } do |f| %>
  <%= render 'form', f: f %>
<% end %>
